{
  "framework": "nextjs",
  "installCommand": "git lfs install && git config lfs.url https://github.com/mcapace/deutsch-hub.git/info/lfs && (git lfs fetch --all && git lfs checkout || echo 'LFS fetch/checkout failed, continuing...') && npm install",
  "buildCommand": "(git lfs pull || echo 'LFS pull failed, continuing build...') && npm run build"
}

